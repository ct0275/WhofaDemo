import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 5,
    borderRadius: 40,
    borderWidth: 1,
    borderColor: "#f5a142",
  },
  image: {
    borderRadius: 40,
    borderWidth: 1,
    borderColor: "#ffffff",
  }
});

export default styles;
